---
name: ui
description: Use the ui-pages agent to build or update UI pages and components (layout, composition, mock data only).
agents:
  - ui-pages
---

Use this command when you want the assistant to focus on **UI composition only** (no new APIs), following the patterns described in `/.cursor/agents/ui-pages.md`.

